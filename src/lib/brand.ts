// brand.ts — the app's name in ONE place. Change it here and it updates everywhere
// (metadata, landing, in-app chrome, emails). Pure module: safe to import on the
// server or the client.
export const APP_NAME = "Caira";
